name: advaita_ticket_validator
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.7.0

dependencies:
  flutter:
    sdk: flutter
  http: ^1.3.0 
  shared_preferences: ^2.5.2  
  fluttertoast: ^8.0.8 
  camera: 
  provider: ^6.1.2
  flutter_secure_storage:
  another_flushbar:
  mobile_scanner: 


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0
  flutter_launcher_icons: 

flutter_launcher_icons:
  android: true
  ios: true
  image_path: "assets/VA.png"

flutter:
  uses-material-design: true
  assets:
    - assets/advaita_logo.png
    